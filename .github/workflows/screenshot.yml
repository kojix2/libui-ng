name: Example screenshots

on:
  push:
    branches: [dev]
    paths:
      - '.github/actions/screenshot/**'
      - '.github/workflows/screenshot.yml'
      - 'common/**'
      - 'darwin/**'
      - 'examples/*.c'
      - 'examples/CMakeLists.txt'
      - 'examples/example.manifest'
      - 'examples/example.static.manifest'
      - 'examples/resources.rc'
      - 'CMakeLists.txt'
      - 'cmake/**'
      - 'ui.h'
      - 'ui_*.h'
      - 'unix/**'
      - 'windows/**'
  workflow_dispatch:

concurrency:
  group: example-screenshots
  cancel-in-progress: true

env:
  WIDGET_EXAMPLES: >-
    button label checkbox entry multiline-entry combobox editable-combobox
    radio-buttons spinbox slider progressbar separator date-picker time-picker
    color-button font-button

jobs:
  screenshot:
    name: ${{ matrix.os }} screenshots
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu
            runner: ubuntu-latest
          - os: windows
            runner: windows-latest
          - os: macos
            runner: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install CMake and Linux dependencies
        if: matrix.os == 'ubuntu'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y cmake ninja-build libgtk-3-dev xvfb imagemagick openbox wmctrl

      - name: Install CMake and macOS dependencies
        if: matrix.os == 'macos'
        run: brew install cmake ninja

      - name: Setup Windows Developer Command Prompt
        if: matrix.os == 'windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Configure build (Unix)
        if: matrix.os != 'windows'
        shell: bash
        run: cmake -S . -B builddir -G Ninja -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF -DLIBUI_BUILD_TESTS=OFF

      - name: Configure build (Windows)
        if: matrix.os == 'windows'
        shell: pwsh
        run: >-
          cmake -S . -B builddir -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DBUILD_SHARED_LIBS=OFF
          -DLIBUI_BUILD_TESTS=OFF
          '-DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded$<$<CONFIG:Debug>:Debug>'

      - name: Build widget examples (Unix)
        if: matrix.os != 'windows'
        shell: bash
        run: |
          for app in $WIDGET_EXAMPLES; do
            cmake --build builddir --target "$app"
          done

      - name: Build widget examples (Windows)
        if: matrix.os == 'windows'
        shell: pwsh
        run: |
          $apps = $env:WIDGET_EXAMPLES -split '\s+'
          foreach ($app in $apps) {
            cmake --build builddir --target $app
          }

      - name: Take screenshots
        uses: ./.github/actions/screenshot
        with:
          os: ${{ matrix.os }}
          app-names: ${{ env.WIDGET_EXAMPLES }}

      - name: Upload screenshots
        uses: actions/upload-artifact@v7
        with:
          name: screenshots-${{ matrix.os }}
          path: '*.png'
          if-no-files-found: error
          retention-days: 30

  push_screenshots:
    name: Update screenshots branch
    needs: screenshot
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Prepare orphan branch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git checkout --orphan screenshots
          git rm -rfq .

      - name: Download screenshots
        uses: actions/download-artifact@v8
        with:
          pattern: screenshots-*
          merge-multiple: true

      - name: Commit screenshots
        run: |
          git add -- '*.png'
          git commit -qm 'Update example screenshots [skip ci]'
          git push -f origin screenshots
