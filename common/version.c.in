#include "ui.h"

#include <string.h>

const char *uiVersion(void)
{
	static const char version[] = "@VCS_TAG@";
	const char *revision = strstr(version, "-g");

	if (revision != NULL)
		return revision + 2;
	return version;
}
